name: Telegram Live Signals

on:
  workflow_dispatch:

concurrency:
  group: live-bot-execution
  cancel-in-progress: true

jobs:
  trade:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install TA-Lib System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential wget
          wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
          tar -xzf ta-lib-0.4.0-src.tar.gz
          cd ta-lib/
          ./configure --prefix=/usr
          make
          sudo make install
          cd ..
          rm -rf ta-lib*

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install freqtrade
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # جایگزینی عبارات موقت با مقادیر واقعی Secrets در کانفیگ
      - name: Inject Telegram Credentials
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          sed -i "s/TELEGRAM_TOKEN_PLACEHOLDER/$BOT_TOKEN/g" config_telegram.json
          sed -i "s/TELEGRAM_CHAT_ID_PLACEHOLDER/$CHAT_ID/g" config_telegram.json

      - name: Run Freqtrade Dry-Run
        run: |
          freqtrade trade --config config_telegram.json --strategy NostalgiaForInfinityX7
